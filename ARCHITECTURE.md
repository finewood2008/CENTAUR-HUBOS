# Centaur Hub：企业级数字员工基站架构白皮书

## 1. 产品定位
Centaur Hub 并非传统的 SaaS 软件，而是一款 **Local-First（本地优先）的软硬一体化设备**，堪称 **“AI 时代的群晖 NAS”**。
它作为一个部署在企业本地的物理中枢，内置算力网关、向量数据库和工具沙盒，企业老板可以通过它一键“入职”多名承担不同职能的独立数字员工（Agents），实现人机同群协作，同时保障核心业务数据绝对不出本地。

---

## 2. 系统架构总览

```text
======================================================================
               ☁️ 云端层：Centaur Cloud (算力结算与路由中心)
======================================================================
       [ OpenAI / Claude / DeepSeek 等大模型 API ]
                        ▲
                        │ (云端网关作为大客户统一支付)
                        ▼
       [ Centaur Gateway (QeeClaw PaaS) ]
         - 校验 Device_Key (设备出厂唯一凭证)
         - 负责 Token 计费、扣除充值的 Credits
         - 拦截异常请求，保护底层真实模型 Key 不泄露
                        ▲
                        │ (仅传输脱敏推理请求，消耗整机电量)
------------------------│---------------------------------------------
                        ▼
======================================================================
               📦 硬件设备层：Centaur-HR Hub (企业本地交付实体)
======================================================================

  [ 🖥️ 中枢控制台：Hub OS (老板/超管面板) ] 
     - 统管算力池，查看各“员工”的消耗报表
     - 员工市场：一键安装预设数字员工
     - Agent Builder：拖拽配置人设、分配数据权限
                        │
                        ▼ (颁发与吊销内部凭证)
                        
  [ 🔑 内部网关与资源池 (Local API Gateway) ]
     - 拦截内部调用，校验 Agent_Key
     - 限流与预算控制 (Quota Management)
     ┌────────────────────────────────────────────────────────┐
     │  [ 🧠 QeeClaw 向量库 ]   [ 🛠️ 工具沙盒 ]  [ 💾 本地模型 ]  │
     │   (存储各部门私有数据)   (执行内部脚本)   (断网兜底推理)   │
     └────────────────────────────────────────────────────────┘
                        │
                        ▼ (沙盒隔离，基于 Agent_Key 鉴权)

  [ 🤖 运行时：数字员工团队 (独立运行的 Agents) ]
     ┌────────────────────────────────────────────────────────┐
     │                     (Event Bus 任务总线)               │
     │                                                        │
     │ [👩‍💼 HR Agent]        [🧑‍🎨 PR Agent]       [💰 财务 Agent]│
     │ 凭证: Agent_Key_01   凭证: Agent_Key_02   凭证: Agent_Key_03│
     │ 配额: 20% 算力       配额: 60% 算力       配额: 20% 算力   │
     │ 权限: 人事 RAG 库    权限: 发稿工具, 搜索  权限: 极密财务库  │
     └────────────────────────────────────────────────────────┘
                        │
------------------------│---------------------------------------------
                        ▼
======================================================================
               👥 用户/外部接入层 (终端触点)
======================================================================
                        │
     ┌──────────────────┼──────────────────┐
     │                  │                  │
 [企业微信/钉钉]   [公司内部系统(OA)]   [本地大屏/语音端]
  (人类@数字员工)  (携带 Agent_Key 调 API)  (直接与 Hub 交互)
```

---

## 3. 核心设计一：双 Key 治理体系 (Dual-Key Architecture)

系统采用了严格的“上下分层、公私分明”的密钥管理体系，解决算力结算与内控问题。

### 3.1 向上结算：Device_Key (设备密钥)
*   **作用**：作为整台 Hub 与云端 Centaur Gateway 进行算力结算和身份认证的唯一凭证。
*   **商业闭环**：客户**无需**自行申请 OpenAI/Claude 的 API Key。硬件出厂即绑定 Device_Key，老板只需在小程序购买“算力电量（Credits）”。设备遇复杂推理需求时请求云端扣除电量，我们作为平台方赚取算力差价。

### 3.2 向下治理：Agent_Key (员工密钥)
*   **作用**：Hub 内部用于管理、寻址和限制各个数字员工的凭证。
*   **预算控制**：老板可为 PR Agent 设定 60% 的算力限额，为 HR Agent 设定 20%。当某个 Agent 耗尽配额时即暂停服务，防止失控，且各部门成本独立核算。

---

## 4. 核心设计二：独立沙盒与权限隔离 (Sandboxing)

**为什么不做一个全能超级 Agent，而必须是多个独立的数字员工？**

1.  **物理级数据安全**：财务 Agent 掌握薪资库，PR Agent 掌握外发稿件库。依靠 `Agent_Key` 的鉴权，确保 PR Agent 绝不可能越权读取到财务的 RAG 数据，从根源切断了 AI 幻觉导致的数据泄露。
2.  **工具防越权**：各数字员工的 Tool 工具链互相隔离。遭受恶意提示词攻击（Prompt Injection）的客服 Agent 无法调用人事 Agent 的“发送辞退邮件”API。
3.  **专注与低成本**：独立 Agent 的 System Prompt 更加专注（不精神分裂），每次对话节省上下文 Token，推理更精准。

---

## 5. 核心设计三：通道路由机制 (Channel Routing)

人类员工无需登录晦涩的硬件后台，日常交互全部发生在企业微信、钉钉等熟悉的通讯工具中。Hub 在此充当 **“企业级 API 路由器”**：

### 5.1 单点私聊模式
*   在企微后台配置多个机器人应用（如：HR助手、公关大使），Webhook 均指向本地 Hub。
*   Hub 接收到消息后，解析来源 AppID，映射至对应的 `Agent_Key`，将请求精准投递至对应数字员工的沙盒。

### 5.2 多智能体协作 (Multi-Agent Collaboration)
*   **群聊模式**：在业务群内同时拉入多名数字员工。Hub 监听群聊，根据 `@对象` 分发指令（例如：`@数据Agent 查数据`，`@公关Agent 写战报`），实现人机同群无缝协作。
*   **后台 Event Bus**：当需要多个 Agent 合作完成复杂任务时，由内部总线（Event Bus）在后台以 JSON 级结构化数据进行任务交接，而非在前端群聊里互相说话，保证协作稳定高效。

---

## 6. 总结与护城河
通过 **Local-First 数据留存本地 + 云端算力按需调度 + 内部严格 Agent 隔离沙盒** 的架构，Centaur Hub 完美解决了企业部署 AI 时最痛的三个点：**数据安全不敢上云、API 配置门槛太高、单一 AI 容易越权胡说。**
它交付的不仅是一台硬件，更是一个标准化的数字化虚拟团队基站。
